package com.mapdir.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette — Teal/Cyan tones ──────────────────────────────────────────
val Primary          = Color(0xFF0D9488)   // Teal 600
val PrimaryContainer = Color(0xFFCCFBF1)   // Teal 100
val OnPrimary        = Color(0xFFFFFFFF)
val OnPrimaryContainer = Color(0xFF0F766E) // Teal 700

// ── Secondary palette — Amber accents ──────────────────────────────────────────
val Secondary          = Color(0xFFF59E0B) // Amber 500
val SecondaryContainer = Color(0xFFFEF3C7) // Amber 100
val OnSecondary        = Color(0xFFFFFFFF)
val OnSecondaryContainer = Color(0xFF92400E)

// ── Tertiary — Indigo for highlights ───────────────────────────────────────────
val Tertiary          = Color(0xFF6366F1)
val TertiaryContainer = Color(0xFFE0E7FF)

// ── Neutral surfaces ───────────────────────────────────────────────────────────
val Background     = Color(0xFFF8FAFC)  // Slate 50
val Surface        = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF1F5F9)  // Slate 100
val OnBackground   = Color(0xFF0F172A)  // Slate 900
val OnSurface      = Color(0xFF1E293B)  // Slate 800
val OnSurfaceVariant = Color(0xFF64748B) // Slate 500
val Outline        = Color(0xFFCBD5E1)  // Slate 300

// ── Error ──────────────────────────────────────────────────────────────────────
val Error          = Color(0xFFEF4444)
val ErrorContainer = Color(0xFFFEE2E2)
val OnError        = Color(0xFFFFFFFF)
val OnErrorContainer = Color(0xFF991B1B)

// ── Dark theme ─────────────────────────────────────────────────────────────────
val DarkPrimary          = Color(0xFF5EEAD4)
val DarkPrimaryContainer = Color(0xFF115E59)
val DarkOnPrimary        = Color(0xFF042F2E)
val DarkOnPrimaryContainer = Color(0xFFCCFBF1)

val DarkSecondary          = Color(0xFFFBBF24)
val DarkSecondaryContainer = Color(0xFF78350F)
val DarkOnSecondary        = Color(0xFF451A03)
val DarkOnSecondaryContainer = Color(0xFFFEF3C7)

val DarkBackground   = Color(0xFF0F172A)
val DarkSurface      = Color(0xFF1E293B)
val DarkSurfaceVariant = Color(0xFF334155)
val DarkOnBackground = Color(0xFFF1F5F9)
val DarkOnSurface    = Color(0xFFE2E8F0)
val DarkOnSurfaceVariant = Color(0xFF94A3B8)
val DarkOutline      = Color(0xFF475569)

// ── Utility colors ─────────────────────────────────────────────────────────────
val StarFilled  = Color(0xFFFBBF24)  // Amber 400
val StarEmpty   = Color(0xFFD1D5DB)  // Gray 300
val DistanceBadge = Color(0xFF0EA5E9) // Sky 500
